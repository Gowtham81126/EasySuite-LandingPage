{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "cd artifacts/landing && npm run dev",
    "build": "cd artifacts/landing && npm run build",
    "typecheck": "cd artifacts/landing && npm run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
